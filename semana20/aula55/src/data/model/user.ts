export type signupInputDTO = {
    name: string,
    nickname: string,
    password: string,
    email: string,
    role: string
}

export type loginInputDTO = {
    email: string,
    password: string
}