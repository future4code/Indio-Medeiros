export type EmailInputDTO = {
    email: string
}

export type UserInputDTO = {
    id: string,
    name: string,
    email: string,
    password: string
 }

 export type UserOutputDTO = {
    id: string,
    name: string,
    email: string,
    password: string
 }
